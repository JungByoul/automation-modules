# 메일 수집 → 관리대장 자동화

**하루 1.2~2.0시간 → 수 초로 단축, 월 23~40시간(0.15~0.25 M/M) 절감**

로컬 Outlook(클래식, COM)의 받은편지함에서 특정 발신자의 승인/등록 알림 메일을 읽어
관리대장 형식의 xlsx로 자동 정리하는 스크립트.

## 하는 일

1. `메일수집_실행.bat` 실행
2. Outlook에 강제 Send/Receive를 걸어 최신 메일까지 동기화
3. 제목에 특정 키워드가 있고 지정 발신자로부터 온 메일만 필터링 (`CUTOFF` 날짜 이후만)
4. 메일 본문에서 표 형태로 박혀있는 항목(ID, 참조자, 발송시각 등)을 파싱
5. 중복 항목은 스킵하고, 접수일·접수시간 기준 정렬해서 xlsx로 저장 (실행마다 타임스탬프 파일로 스냅샷)
6. 결과 파일과 폴더를 자동으로 열어줌

## 실행 조건

- Windows + 로컬 클래식 Outlook 설치 (COM 자동화 사용, 인증/MCP 불필요)
- Python 3.x, `pip install -r requirements.txt`
- 스크립트 상단의 `SRC`, `BASE`, `SENDER_KEY`, `CUTOFF` 값을 실제 환경에 맞게 수정

## 파일

| 파일 | 역할 |
|---|---|
| `collect_moc_mail.py` | 핵심 스크립트. Outlook 스캔 + 파싱 + xlsx 생성 |
| `메일수집_실행.bat` | 실행용 (더블클릭) |

## 자동화 효과 (세부 내역)

인수인계(2026-05-26) 전까지는 항상 수기로 진행되던 업무. 인수인계 후에도 한동안 그대로 수기로
하다가, 반복작업이 너무 많고 누락도 잦아서 이 스크립트를 만듦. 자동화 대상은 "접수 기록" 단계까지고,
이후 MDM에서 오류 확인·승인하는 단계는 여전히 사람이 함.

### 수기로 할 때 1건당 거치는 단계

메일 열기·접수 확인 → 관리대장 열기·접수일 입력 → 메일 재확인·구분 판단 → Workflow ID/설비ID 복붙(표 셀 단위) →
MDM 로그인 → 변경/삭제 화면 이동 → 리스트에서 설비ID 검색·클릭 → 탭 클릭해서 담당자 이름 확인 → 관리대장에 이름 붙여넣기

| 숙련도 | 1건당 시간 |
|---|---|
| 숙련자 | 2.3분 |
| 신입(초기) | 4분 |

### 실제 접수 물량 (접수대장 데이터 기준)

| 구분 | 기간 | 영업일 | 총 건수 | 일평균 | 최대(하루) |
|---|---|---|---|---|---|
| 제 근무 기간 | 2026-05-26 ~ 07-28 | 44일 | 477건 | 10.8건 | 47건 |
| 인수인계 이전 9개월 | 2025-08 ~ 2026-05 | 191일 | 5,789건 | 30.3건 | 359건 |

### 시간 절감 — 제 근무 기간 기준 (일평균 10.8건)

| 숙련도 | 하루 절감시간 |
|---|---|
| 숙련자 | 24.8분 |
| 신입 | 43.2분 |

### 시간 절감 — 가장 많이 들어온 날 기준 (하루 47건)

| 숙련도 | 그 날 절감시간 |
|---|---|
| 숙련자 | 108분 (1.8시간) |
| 신입 | 188분 (3.1시간) |

### 기대효과 — 인수인계 이전 9개월 평균 기준 (일평균 30.3건)

| 숙련도 | 하루 절감시간 | 월 절감시간(20영업일 기준) | M/M |
|---|---|---|---|
| 숙련자 | 69.7분 (1.2시간) | 23.2시간 | 0.15 |
| 신입 | 121.2분 (2.0시간) | 40.4시간 | 0.25 |

> M/M은 월 20영업일 × 1일 8시간(160시간) 기준으로 환산.
